# Campaign Log — 28-vCPU Linux VPS, 2026-04-27

Snapshot of the dedicated-VPS campaign run launched in response to the prior
session's tier-1/2 recommendations (multi-day Cirbo SAT + parallel eSLIM
sweep).

## Hardware

- 28 vCPU / 56 GB RAM Ubuntu 22.04 box
- 28 cores allocated as: 18 eSLIM sweep, 7 Cirbo per-bit, 1 Cirbo
  conditional-negate sub-block, 2 reserved for monitor/git

## Toolchain (validated working)

- yosys 0.51+104 (OSS CAD Suite 2025-04-01)
- ABC 1.01 (Apr 2025 build, bundled with OSS CAD Suite)
- Cirbo 1.0.0 + pysat 1.9.dev2 (cadical195/153, lingeling, glucose42, ...)
- eSLIM (current main, fxreichl/eSLIM) — Linux build needed no APFS hack
- kissat 4.0.4 — built but not yet wired to Cirbo

## Running (as of 2026-04-27 04:40 UTC)

### eSLIM sweep round 1
- 140 experiments × 18-way parallel. Started ~03:46, restarted at 04:21
  after a process-management bug.
- Current best: **65 contest cells** (canonical re-discovery, 58 internal,
  7 NOTs).
- All 45 ok results so far ≥ 65. None below.
- ETA: ~05:55 UTC for final completion.

### Cirbo per-bit lower bounds
- Per-bit walk-up campaign at G=7 with 4-hour budget per Y[k].
- Y[0] = 4 proven (prior session)
- Y[8] = 7 proven (this session)
- Y[1..7]: ≥ 7 proven (G=6 UNSAT in 55-105s); G=7 attempt active.

### Cirbo conditional-negate sub-block
- 9-input × 7-output single-output SAT search, walking G upward.
- Lower bound proven so far: **≥ 11 gates** (UNSAT G=8/9/10).
- G=11 attempt active.

## Round 2 plan (queued)

When round 1 finishes:

- **Phase A** (84 expts × 1500s, ~2hr wall): all 12 topology variants +
  canonical_65/70 × sizes {8, 10} × seeds {1, 42, 7777}. Goal: explore
  diverse local basins; the variants are gate-neutral rewrites of the
  canonical that may converge differently in eSLIM.
- **Phase B** (16 expts × 3600s, ~1hr wall): canonical_65 with sizes
  {12, 14}, restarts {0, 5}, seeds {1, 42, 7777, 42424242}. The
  unexplored axis: extreme window sizes with restarts.
- **Phase C** (16 expts × 3600s, ~1hr wall): canonical_70 with same.

Phase A first. If anything < 65 emerges, promote and skip B/C. If not,
run B/C overnight.

## Topology variants (round 2 phase A starting points)

12 gate-neutral rewrites of the canonical 65/70 generated by
`workspace/eslim_runs/gen_variants.py`:

- **canonical_65_pushed** — manually applied `not_77 = XOR(not_70, w_38)`
  (NOT-pushing through XOR, exploiting existing not_70). Cell mix changes
  from 25/12/21/7 (AND/OR/XOR/NOT) to 25/12/22/6 — same total, fewer NOTs.
- **fp4_mul_v1_pushA_not_77** — auto-generated equivalent.
- **fp4_mul_v2_assocA_w_26 / w_75 / assocB_w_72** — XOR re-association
  rewrites (XOR(XOR(a,b),c) → XOR(a,XOR(b,c))).
- 7 similar variants on canonical_70.

## What's still in flight beyond round 2

- Multi-day Cirbo SAT on the FULL circuit at G=64 — currently infeasible
  on this 56GB machine (CNF build alone consumes ~25GB before solving).
  Would need either (a) a 128GB+ RAM box with kissat-direct CNF solving,
  or (b) Cirbo's `forbid_wire`-based incremental search with smarter
  heuristics.
- AlphaEvolve LLM mutation loop — requires Anthropic API budget that is
  not available in this session.
